{
  "name": "bootstrap_app",
  "version": "1.0.0",
  "description": "bootstrap using application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "styles:w": "sass client/styles/scss/base.scss client/styles/dist/styles.css --watch --load-path=node_modules/bootstrap/scss/",
    "server:dev": "nodemon app.js",
    "server": "export NODE_ENV=production && sudo -E \"$(which node)\" app.js",
    "sync:aws": "rsync -rvP -e ssh --exclude=node_modules --exclude=.git ./ ubuntu@3.123.1.152:/home/ubuntu/hooli-network"
  },
  "author": "Miha Go",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.56.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.2",
    "bootstrap-icons": "^1.10.2",
    "config": "^3.3.8",
    "express": "^4.18.2",
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5",
    "run-all": "^1.0.1"
  }
}
